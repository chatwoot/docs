backend:
  name: github
  branch: main
  repo: chatwoot/docs

# These lines should *not* be indented
media_folder: "static/img" # Media files will be stored in the repo under static/images/uploads
public_folder: "/img/" # The src attribute for uploaded media will begin with /images/uploads

collections:
  - name: docs
    label: Docs
    folder: docs
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }

  - name: contributing-guide
    label: "Contributing Guide"
    folder: docs/contributing-guide
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    nested:
      depth: 100
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }

  - name: handbook
    label: "Handbook"
    folder: docs/handbook
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    nested:
      depth: 100
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }

  - name: product
    label: "Product"
    folder: docs/product
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    nested:
      depth: 100
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }

  - name: user-guide
    label: "User Guide"
    folder: docs/user-guide
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    nested:
      depth: 100
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }

  - name: self-hosted
    label: "Self Hosted"
    folder: docs/self-hosted
    identifier_field: title
    extension: md
    widget: "list"
    create: true
    slug: "{{slug}}"
    nested:
      depth: 100
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
      - { name: sidebar_label, label: Sidebar Label, widget: string }
